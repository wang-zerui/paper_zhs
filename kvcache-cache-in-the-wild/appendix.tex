\section{Appendix}

% \subsection{Trace Analyze}

%\TODO{Some explaination about the changes of figs}

% We analyzed more traces sampled from Alibaba Tongyi Application at April 2024. 
% The main difference with these traces is that 
% they do not include "Search" type but instead include "Agent" type for TraceA,
% and they include "Text" type in TraceB. 
% The "Agent" type stands for the role-play service that allows users
% to interact with LLM acting as specific roles.
% The "Text" type in TraceB is similar to the "Text" type in TraceA, which is ChatBot.
% Moreover, these traces contain almost no system prompts in the single-turn requests.

% \fig{fig:motiv-next-turn-probability-old} shows the next-turn probability in the peak time in a day.
% We can see that the frequency of issuing the next-turn request remains consistent over time (shown by horizontal lines),
% means the next-turn probability is predictable.

% \fig{fig:motiv-spatial-workload-old} shows the temporal locality for single-turn requests.
% We can see that there are almost no reuse between single-turn requests in all workload types,
% which differs from the analysis in \fig{fig:motiv-spatial-workload}. This is because
% the trace contain almost no system prompts in the single-turn requests.

% \fig{fig:motiv-temporal-workload-old} shows the temporal locality of this traces. 
% As we can see, less than 3\% of the single-turn requests are reusable in this Trace because
% of the rare system prompt.

% \fig{fig:motiv-reuse-time-probability-old} shows the timeline 
% of the mean reuse time for different request categories in a heatmap. 
% As in the previous section, we consider the 1-turn and 2-turn requests as distinct request categories. 
% The intensity of the color indicates the normalized reuse time,
% e.g., lighter colors indicate shorter reuse time. 
% Although different categories of requests have distinct reuse times (the vertical color blocks), 
% the reuse time for a specific category remains stable over time (the horizontal color blocks),
% which aligns with the analysis in \ref{analysis:block-reuse-probability}.

% \fig{fig:motiv-7days} shows the evolution of the reuse probability
% for each request category in future time window over 4 continous workdays.
% We can observe that the reuse probability in 9:00 am to 10:00 am
% similar between 4 workdays. Therefore, we can analyze the reuse log
% of last day offline to predict the reuse probability of each category 
% in the next day. However, it is noted that the reuse probability of 
% "image" type is harder to predict than other categories, 
% we infer that this is because image types have coarser reusability granularity 
% compared to text types, and there exists greater variability among different users.

% \begin{figure}[!t]
%     %\hspace{-3mm}
%     \centering
%     \includegraphics[width=0.9\linewidth,center]{./data/next-turn-probability-old\figsuffix}  \\[-1pt]
%     \begin{minipage}{1\linewidth}
%     \caption{\small{\emph{        
%     %An analysis of the frequencies of issuing the next turn request given a request type. 
%     An illustration of how the frequencies of issuing the next turn request change over time 
%     for different request types in the peak time of a workday.
%     Note that the x-axis is the timeline.
%     The workload types are sorted according to their usage.
%     }}}
%     \label{fig:motiv-next-turn-probability-old}
%     \end{minipage} \\[-5pt]
% \end{figure} 

% \begin{figure}[!t]
%     %\hspace{-3mm}
%     \centering
%     \includegraphics[width=0.9\linewidth,center]{./data/temporal-workload-old\figsuffix}  \\[-1pt]
%     \begin{minipage}{1\linewidth}
%     \caption{\small{\emph{        
%         Temporal locality for single-turn requests on both traces.
%             }}}
%     \label{fig:motiv-temporal-workload-old}
%     \end{minipage} \\[-5pt]
% \end{figure} 

% \begin{figure}[]
%     %\hspace{-3mm}
%     \centering
%     \includegraphics[width=0.9\linewidth,center]{./data/reuse-time-probability\figsuffix}  \\[-1pt]
%     \begin{minipage}{1\linewidth}
%     \caption{\small{\emph{        
%         An illustration of how the mean reuse time of 
%         {\kvcache} blocks changes over time for different request categories. 
%         Note that the x-axis represents the timeline.
%         The request categories are sorted according to their usage.
%     }}}
%     \label{fig:motiv-reuse-time-probability-old}
%     \end{minipage} \\[-5pt]
% \end{figure}

% \begin{figure*}[!t]
%     %\hspace{-3mm}
%     \centering
%     \includegraphics[width=0.9\linewidth,center]{./data/spatial-workload\figsuffix} \\[0pt]
%     \begin{minipage}{1\linewidth}
%     \caption{\small{\emph{
%         The spatial locality of single-turn requests. 
%     }}}
%     \label{fig:motiv-spatial-workload-old}
%     \end{minipage} \\[-5pt]
% \end{figure*}

% \begin{figure}[!t]
%     %\hspace{-3mm}
%     \centering
%     \includegraphics[width=1.0\linewidth,center]{./data/spatial-workload\figsuffix} \\[0pt]
%     \begin{minipage}{1\linewidth}
%     \caption{\small{\emph{
%         The spatial locality of single-turn requests. 
%     }}}
%     \label{fig:motiv-spatial-workload-old}
%     \end{minipage} \\[-5pt]
% \end{figure}


% \begin{figure*}[!t]
%     %\hspace{-3mm}
%     \centering
%     \includegraphics[width=0.9\linewidth,center]{./data/reuse-time-probability-distribution-7days-2\figsuffix} \\[0pt]
%     \begin{minipage}{1\linewidth}
%     \caption{\small{\emph{
%         The evolution of reuse probability in 5 workdays.
%     }}}
%     \label{fig:motiv-7days}
%     \end{minipage} \\[-5pt]
% \end{figure*}


\subsection{Trace results across different days}
\label{analysis:reuse-evolution}

\begin{figure*}[!t]
    %\hspace{-3mm}
    \centering
    \includegraphics[width=1.0\linewidth,center]{./data/reuse-time-probability-distribution-7days-2\figsuffix} \\[5pt]
    \begin{minipage}{1\linewidth}
    \caption{\small{\emph{
        The evolution of cumulative distribution function of the reuse probability in 5 workdays.
        The first row: distributions from 9.00 a.m. to 10.00 a.m.
        The second row: distributions from 10.00 a.m. to 11.00 a.m.
    }}}
    \label{fig:motiv-7days}
    \end{minipage} \\[-5pt]
\end{figure*}

\noindent
\fig{fig:motiv-7days} shows the evolution of cumulative distribution (CDF) function of 
the reuse probability for each request category in future time window over 5 continuous workdays.
We can observe that the reuse probability at 9:00 a.m. to 10:00 a.m.
similar between workdays. Therefore, we can analyze the reuse log
of last day offline to predict the reuse probability of each category 
in the next day. However, it is noted that the reuse probability of 
"image" type is harder to predict than other categories, 
we infer that this is because image types have coarser reusability granularity 
compared to text types, and there exists greater variability among different users.