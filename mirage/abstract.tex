\begin{abstract}
We introduce \sys, the first multi-level superoptimizer for tensor programs. 
%A key idea behind \sys is \graph, a holistic approach to representing algebraic and schedule transformations at the kernel, thread block, and thread levels of the GPU compute hierarchy, allowing \sys to discover novel optimizations beyond the search space of prior work.
A key idea in \sys is \graphs, a uniform representation of tensor programs at the kernel, thread block, and thread levels of the GPU compute hierarchy.
\graphs enable \sys to discover novel optimizations that combine algebraic transformations, schedule transformations, and generation of new custom kernels.
%
To navigate the large search space, \sys introduces a pruning technique based on abstraction that significantly reduces the search space and provides a certain optimality guarantee.
%guaranteeing the optimality of the discovered \graphs.
To ensure that the optimized \graph is equivalent to the input program, \sys introduces a probabilistic equivalence verification procedure with strong theoretical guarantees.
Our evaluation shows that \sys significantly outperforms existing approaches even for DNNs that are widely used and heavily optimized.
\iffinal
\sys is publicly available at \url{https://github.com/mirage-project/mirage}.
\fi 
% by existing systems.
%\oded{Maybe emphasize that it discovers new kernels, and also mention attention}
%\oded{I would slightly deemphasize the abstract expressions and the probabilistic verification from the abstract. (Maybe ``rigorous theoretical results'' is too much for what the paper actually contains since we don't include the proofs and all the steps, and also not the focus of the SOSP audience.)}
\end{abstract}

%Candidates of names for our system:

%MISO?

% From ChatGPT: TensorOpt, MultiTensor, TensorPrime, TensorBoost, TensorTune, TensorMax, TensorAccel, TensorFusion, TensorHarmony, $\mu$Transformer, $\mu$Tensor

% From word search: Musu, Muso, Mull, Mutes, Soul, Suite, Summer
