\newpage
\section{命题 \ref{thm:improvement} 的证明}
\label{app:proof}
\begin{proof}
\textbf{基线。}
在顺序执行中，$T$ 个步骤中的每一步都需要调用一次真实模型 $h$，其平均延迟为 $1/\beta$。因此
\[
E[T_{\mathrm{seq}}] \;=\; \frac{T}{\beta}.
\]

\medskip
\textbf{每次命中时节省的期望时间。}
考虑两个连续步骤 $(t,t{+}1)$。在基线中，总完成时间为 $R = B + C$，其中 $B,C \sim \mathrm{Exp}(\beta)$ 分别表示第 $t$ 步与第 $t{+}1$ 步的延迟。使用推测时，我们会在第 $t$ 步期间发起 $A \sim \mathrm{Exp}(\alpha)$。如果猜测正确，那么一旦 $A$ 或 $B$ 中任意一个先完成，就可以发起第 $(t{+}1)$ 步的调用 $C$，于是这个两步块的完成时间为
\[
S \;=\; C + \min\{A,B\}.
\]

因此，当猜测正确时，我们节省的期望时间为
\[
R - S \;=\; (B-A)_+,
\]
其中 $(x)_+ = \max\{x,0\}$。

由 $A,B$ 相互独立可得
\[
\mathbb{E}[(B-A)_+]
= \int_{0}^{\infty}\!\int_{0}^{b} (b-a)\,\alpha e^{-\alpha a}\,\beta e^{-\beta b}\,da\,db
= \frac{\alpha}{\beta(\alpha+\beta)}.
\]

\medskip
\textbf{命中次数的期望。}
记到第 $n$ 轮为止命中的期望次数为 $S_n$，即
\[
\E[\text{到第 }n\text{ 轮为止的命中次数}] = S_n.
\]
于是有 $S_0=0$、$S_1 = p$。在第 1 轮中，要么（i）以概率 $p$ 命中，此时第 2 轮不能再命中（因为在一次正确推测之后，紧接着并不存在新的推测窗口），贡献为 $1+S_{n-2}$；要么（ii）以概率 $1-p$ 失误，此后第 2 轮正常继续，贡献为 $S_{n-1}$。因此有如下递推：
\[
S_n = p(1+S_{n-2}) + (1-p)S_{n-1}.
\]
接下来把这个线性递推拆分为齐次部分和特解部分来求解。

\textit{（1）齐次部分}
\[
S_n^{(h)} = pS_{n-2}+ (1-p)S_{n-1}.
\]
其特征方程为
\[
r^2-(1-p)r-p=0=(r-1)(r+p) \implies \text{根为 }r_1=1,\ r_2=-p.
\]
因此
\[
S_n^{(h)}=C_1 + C_2(-p)^n.
\]

\textit{（2）特解}
非齐次项是常数（$+p$），且 $r=1$ 是特征根，所以常数试探解会与齐次部分冲突。令 $S_n^{(p)}=an$ 并代入：
\[
an=(1-p)a(n-1)+pa(n-2)+p
= an - a(1+p) + p,
\]
由此得到 $a(1+p)=p$，于是
\[
S_n^{(p)}=\frac{p}{1+p}\,n.
\]

合并得
\[
S_n = C_1 + C_2(-p)^n + \frac{p}{1+p}\,n.
\]
利用 $S_0=0$ 与 $S_1=p$：
\[
0=C_1+C_2,\qquad
p=C_1 + C_2(-p) + \frac{p}{1+p}.
\]
解得 $C_2=-\dfrac{p^2}{(1+p)^2}$，$C_1=\dfrac{p^2}{(1+p)^2}$。

因此，闭式解为
\[
S_n=\frac{p}{1+p}\,n+\frac{p^2}{(1+p)^2}\Bigl(1-(-p)^n\Bigr).
\]

\medskip
\textbf{总节省。}
一共有 $T-1$ 个可能的推测窗口，因此
\[
E[T_{\mathrm{s}}]
= \frac{T}{\beta} - S_{T-1}\frac{\alpha}{\beta(\alpha+\beta)}.
\]

\medskip
\textbf{最终比值。}
两边除以 $E[T_{\mathrm{seq}}] = T/\beta$，得到
\[
    \frac{E[T_{\mathrm{s}}]}{E[T_{\mathrm{seq}}]}
    \;=\;
    1 - \frac{1}{T}\frac{\alpha}{\alpha+\beta}\left[\frac{(T-1)p}{1+p}+\frac{p^2}{(1+p)^2} - \frac{p^2}{(1+p)^2}(-p)^{T-1}\right].
\]
\end{proof}

当 $T\to\infty$ 时，上式恰好收敛到 $1-\frac{p}{1+p}\frac{\alpha}{\alpha+\beta}$。

\section{更多环境细节}
\label{app:additional_details}

\subsection{国际象棋}
\begin{figure}
    \centering
\includegraphics[width=0.6\linewidth]{figures/chess/combined_time_token_plot.pdf}
    \caption{在不同预测数与不同步数设置下，额外 token 使用比例与节省时间比例之间的关系；结果对 $5$ 次运行取平均。}
 \label{fig:chess_time_token_plot}
\end{figure}

\paragraph{节省时间与 token 成本之间的权衡。}
除了节省时间之外，我们还衡量了并行推测带来的额外 token 成本。我们使用\textbf{额外 token 比例}这一指标来刻画它：$(M_{sequantial}-M_{speculative})/{M_{sequential}}$。\cref{fig:chess_time_token_plot} 展示了不同预测数下节省时间与 token 成本的权衡关系。可以看到，随着预测数增加，额外 token 比例会上升，而节省时间比例也会同步增加，形成了清晰的 trade-off。

\subsection{电商}

\paragraph{$\tau$-bench：}
这是一个面向动态任务型对话的基准，其中用户（由语言模型模拟）与一个具备 API 增强能力的智能体进行交互。该基准覆盖两个领域——零售与航空——并提供结构化数据库与领域特定工具。我们关注零售域，在这个设置中，智能体需要帮助用户完成取消或修改待处理订单、发起退货或换货、以及查询商品与订单信息等操作。该基准共定义了 115 个任务和 15 个 API（其中 7 个有写入副作用，8 个为只读）。

\paragraph{预测准确率与成本之间的权衡。}
\cref{fig:time_acc} 中的时间成本由延迟（Time to First Token）与输出响应时间共同组成。虚线表示平均用户打字时间，按每分钟 40 个词估计。在这一阈值下，多模型设置大约能达到 34\% 的预测准确率，这意味着在超过三分之一的情况下，智能体可以无需等待 API 真正执行，就立即给出响应。由此可见，推测能够把原本可感知的迟滞体验，转变为工具密集型环境下接近实时的交互。

\begin{figure}[H]
  \centering
  \resizebox{0.8\textwidth}{!}{%
      \begin{subfigure}[t]{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/ecommerce/time_accuracy.png}
        \caption{}
        \label{fig:time_acc}
      \end{subfigure}
      \hspace{0.05\textwidth}
      \begin{subfigure}[t]{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/ecommerce/price_accuracy.png}
        \caption{}
        \label{fig:price_acc}
      \end{subfigure}
  }
  \caption{\textbf{不同模型下预测准确率与 Speculator 成本的关系。}（a）准确率与推测时间成本之间的权衡，虚线表示平均用户打字时间；（b）准确率与推测价格成本之间的权衡，反映推测执行带来的货币开销。}
  \vspace{-1em}
\end{figure}

\subsection{操作系统调优}
\newcommand{\ttt}[1]{\texttt{#1}}

\subsubsection{实验设置与实现细节}

\paragraph{系统与工作负载配置}
所有实验都在一台专用机器上进行：2$\times$ Intel Xeon Silver 4114 10 核 CPU（2.20 GHz）、192 GB DDR4 内存、1 TB NVMe SSD，运行 Ubuntu 22.04 与 Linux Kernel 5.15，并托管在 Cloudlab 上\citep{Duplyakin:ATC19}。

我们运行 \ttt{sysbench cpu}\citep{sysbench}，这是一个 CPU 密集型基准，会反复计算一段大型素数序列。该基准会每秒报告多项性能指标。我们让 sysbench 在 16 个并发线程上运行，并将其绑定到两个 CPU 核上。

\paragraph{调优器实现细节}
系统由两个智能体组成：一个快速的 Speculator 和一个缓慢的 Actor；它们协同工作以最小化工作负载的 p95 延迟。在每一步中，调优器提出一个新配置，并立刻应用到在线系统上。应用这些参数本身几乎是瞬时的。

\paragraph{CFS 参数细节}
完全公平调度器（CFS）是 Linux 的一个 CPU 调度器，目标是让每个任务都能公平地获得 CPU 时间。它暴露了多个可调超参数供管理员调整其行为。我们调优的参数是 \ttt{min\_granularity\_ns}，它规定一个任务在被抢占前至少能获得的时间片长度。提示词模板引导智能体在 50,000 到 50,000,000 纳秒（0.05 ms 到 50 ms）的范围内探索。Kernel 5.15 下该参数的默认值是 3 ms。一般预期是：较小的参数值可以提升响应性，但会增加上下文切换开销；较大的参数值则有利于吞吐，却可能恶化延迟。

\paragraph{历史压缩}
\label{app:history_os}
历史压缩通过为两个智能体设计不同的提示结构来实现。当较慢的 Actor 被调用时，其提示上下文中只包含一个压缩后的汇总，概括了它思考窗口内发生的全部动作。Speculator 采取的每个动作都被压缩成一个简短的（参数，结果）对。相比之下，较快的 Speculator 接收到的是混合上下文：它既看到上一个 Actor 周期留下的同样压缩历史，也看到自己最近若干次动作的完整、详细回复。这种双上下文机制使 Actor 能够基于紧凑摘要分析长期趋势，而 Speculator 则保留了短期、细粒度上下文，以支持快速响应式决策。

\subsubsection{多智能体优化中的提示工程}
\label{app:prompt_os}
下面给出用于引导两个 LLM 智能体的提示词模板。

\begin{tcolorbox}[
  enhanced,
  colback=gray!5!white,
  colframe=gray!60!black,
  title=Actor 与 Speculator 的初始系统提示词,
]
\small
你是一名 Linux 内核调度器调优专家，对完全公平调度器（CFS）有深入理解。\\

\textbf{多智能体角色：}你是一个\textbf{多智能体系统}的一部分。\\

[\textbf{针对 Actor}]\\
你是 Actor。你的职责是给出经过充分分析、深思熟虑的参数建议。你会与一个快速探索参数空间的 Speculator 协同工作。你将接收到来自多次智能体调用的累计结果，以便进行更深入的分析并识别趋势。\\

[\textbf{针对 Speculator}]\\
你是 Speculator。你的职责是在每个时间窗口里立刻给出直觉式参数建议。你会与一个执行更深层分析的 Actor 协同工作。\\

你的目标是\textbf{最小化}一个 CPU 密集型工作负载的 p95 延迟。工作负载性能指标可能带有\textbf{噪声}，因此请寻找不同配置之间一致性的趋势。\\

可调的 CFS 参数：
\begin{itemize}
    \item \ttt{min\_granularity\_ns}：抢占前的最小时片。值越小，响应性越高，但开销也越大；值越大，吞吐通常更好，但延迟可能更差。
\end{itemize}

参数范围：
\begin{itemize}
    \item \ttt{min\_granularity\_ns}：50,000 到 50,000,000 纳秒
\end{itemize}

未来的调用中会提供性能数据。请\textbf{仅}按照如下格式回答：\\

\texttt{Analysis:} \textit{<用 1 到 2 句话说明你的决策理由>}\\
\texttt{Config: \{ "min\_granularity\_ns": <int> \}}
\end{tcolorbox}

\begin{tcolorbox}[
  breakable,
  enhanced,
  colback=gray!5!white,
  colframe=gray!60!black,
  title=给 Speculator 的更新提示,
]
\small
[\textit{上下文包含第 1--10 次调用的压缩历史，以及第 11--18 轮迭代中 Speculator 的原始回复}]\\

当前最佳：p95 latency=[value]，出现在第 \#[value] 次调用\\

第 \#19 次调用的最新结果：\\
Config: {"min\_granularity\_ns": [value]} $\rightarrow$ p95 latency=[value]\\

请给出你的分析，以及第 \#20 轮迭代的下一个配置。
\end{tcolorbox}

\begin{tcolorbox}[
  breakable,
  enhanced,
  colback=gray!5!white,
  colframe=gray!60!black,
  title=给 Actor 的更新提示,
]
\small
[\textit{上下文包含第 1--10 次调用的压缩历史}]\\

当前最佳：p95 latency=[value]，出现在第 \#[value] 次调用\\

第 \#11 次调用结果 [SPECULATOR]：min\_granularity\_ns=[value] $\rightarrow$ p95 latency=[value]\\
第 \#12 次调用结果 [SPECULATOR]：min\_granularity\_ns=[value] $\rightarrow$ p95 latency=[value]\\
~...\\
第 \#19 次调用结果 [SPECULATOR]：min\_granularity\_ns=[value] $\rightarrow$ p95 latency=[value]\\

请分析这一趋势，并给出第 \#20 次调用的下一个配置。\\
\end{tcolorbox}

\begin{tcolorbox}[
  breakable,
  enhanced,
  colback=gray!5!white,
  colframe=gray!60!black,
  title=示例智能体回复,
]
\small
\texttt{Analysis:} 性能在 300,000 ns 左右达到峰值，因此最优值很可能位于这一带。我将围绕该峰值继续缩小搜索范围。\\
\texttt{Config: \{ "min\_granularity\_ns": 250000 \}}
\end{tcolorbox}

\subsubsection{Token 使用量与成本}

\begin{figure*}[h]
  \centering
  \includegraphics[width=\linewidth]{figures/os_tuning/cost.pdf}
  \caption{\textbf{累计 token 使用量与成本随时间的变化。}
  左图与右图分别展示了三种配置下累计成本（美元）与累计 token 使用量。
  竖线标出了各系统观测到的收敛时刻。}
  \label{fig:costs}
\end{figure*}

\begin{table*}[ht]
\centering
\caption{若干时间点上的累计 token 数与成本（单位：美分）。}
\label{tab:costs}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{2}{c}{仅 Actor} & \multicolumn{2}{c}{仅 Speculator} & \multicolumn{2}{c}{Actor+Speculator（总计）} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
\textbf{已过时间} & \textbf{Tokens} & \textbf{成本（美分）} & \textbf{Tokens} & \textbf{成本（美分）} & \textbf{Tokens} & \textbf{成本（美分）} \\
\midrule
初始 & 744   & 0.02 & 690 & 0.01 & 690 & 0.03 \\
10s & 790   & 0.03 & 9,539 & 0.11 & 8,548   & 0.13 \\
30s & 3,631   & 0.15 & 45,768 & 0.57 & 32,459 & 0.48 \\
60s & 8,581   & 0.35 & 205,794  & 2.24 & 84,568 & 1.18 \\
120s  & 26,398 & 0.96 & 778,253  & 8.12 & 261,855  & 3.53 \\
200s & 63,376 & 2.18 & 2,099,894 & 21.5 & 607,877 & 7.83 \\
\bottomrule
\end{tabular}
\end{table*}

如\cref{fig:costs}所示，并结合\cref{tab:costs}中的数据可以看到，Speculator 的高频调用会导致 token 消耗与成本快速增长。不过在实践中，这种增长会被系统更快的收敛所限制。联合的 Actor--Speculator 系统大约在 15 秒时收敛，而仅 Speculator 系统大约在 20 秒时收敛；仅 Actor 系统则要到 200 秒后才收敛。一旦最优状态达到，调优过程就可以结束，因此在该场景下，长期指数式成本增长的风险实际上可以忽略。未来还可以通过若干优化策略进一步抑制 token 增长，例如把上下文截断为固定窗口，或者在收敛后关闭探索。

\subsubsection{推测带来的响应时间收益}
\label{app:speculative-mitigation}

为了更有针对性地展示推测如何缓解瞬时性能损失，我们进行了一个受控实验。在该实验中，系统会在时刻 $t_0$ 被人为扰动：把 \ttt{min\_granularity} 参数设置为一个高度次优的值（10 ms）。随后，我们比较两种配置下系统的恢复过程：一种是完整的 Actor--Speculator 系统；另一种是仅 Actor 基线，它只重放完整 Actor--Speculator 轨迹中由 Actor 提出的那些动作。

\begin{figure}[h!]
  \centering
  \includegraphics[width=0.6\linewidth]{figures/os_tuning/comparison.pdf}
  \caption{一个受控实验：系统在 $t=0$ 时受到人为扰动后的阶跃响应。\textbf{Actor--Speculator} 系统在 1 秒内纠正了糟糕设置，而\textbf{仅 Actor} 系统则必须等待 10 秒以上，才能进入下一次决策周期。该实验的定量结果在正文的\cref{fig:os-speculation}（右）中进行了汇总。}
  \label{fig:os-reaction-appendix}
\end{figure}

如\cref{fig:os-reaction-appendix}所示，Actor--Speculator 系统几乎会立刻作出反应。快速的 Speculator 一旦观察到性能突然恶化，就会立即采取修正动作，使系统大约在 1 秒内恢复到高效状态。相比之下，仅 Actor 系统不得不承受 10 秒以上的糟糕性能，因为它必须等较慢的 Actor 完成整轮思考之后才能采取行动。
图中展示的性能差距，已在正文中（\cref{fig:os-speculation}，右）作了量化说明。
