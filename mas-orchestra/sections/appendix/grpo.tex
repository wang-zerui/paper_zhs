\section{Policy Optimization in \ourframework}
\label{ap:grpo}

The policy in \cref{eq:general_objetive} is optimized by maximizing
\begin{equation}
\label{eq:grpo}
\begin{aligned}
&J_{\text{GRPO}}(\theta)
=\;\\
&\mathbb{E}_{x \sim \mathcal{D},\, \{a_i\}_{i=1}^{K} \sim \pi_{\theta_{\text{old}}}(\cdot \mid x,m)}
\frac{1}{K}\sum_{i=1}^{K}\frac{1}{|a_i|}\sum_{t=1}^{|a_i|}
\Bigg\{
\min\!\Bigg[
r_{i,t}(\theta)\,
\hat{A}_{i,t},\;
\mathrm{clip}\!\Bigg(
r_{i,t}(\theta),\,
1-\epsilon,1+\epsilon
\Bigg)\hat{A}_{i,t}
\Bigg]
-\;\beta\, D_{\mathrm{KL}}\!\left(\pi_\theta \,\|\, \pi_{\mathrm{ref}}\right)
\Bigg\}.
\end{aligned}
\end{equation}

where
\begin{equation}
r_{i,t}(\theta)
=
\frac{\pi_\theta(a_{i,t}\mid x, a_{i,<t}, m)}
{\pi_{\theta_{\text{old}}}(a_{i,t}\mid x, a_{i,<t}, m)}.
\end{equation}
For outcome-supervised training, we assign a sequence-level group-normalized advantage
to all tokens in $a_i$:
\begin{equation}
\hat{A}_{i,t}=\hat{R}_i,
\qquad
\hat{R}_i
=
\frac{R_i - \mathrm{mean}(\{R_j\}_{j=1}^K)}
{\mathrm{std}(\{R_j\}_{j=1}^K)}.
\end{equation}
where $\bar{R} = \frac{1}{K}\sum_{i=1}^K R_i$ is the group-wise average reward,
and $\theta_{\text{old}}$ denotes the parameters of the policy used to generate
the samples.
% \footnote{The KL term used in practice is omitted for simplicity.} 
% \shafiq{I think GRPO uses $\sigma$. Also, should we use token level? The placement of the length normalization also matters.} 
% \yf{This objective seems quite different from GRPO. (1) Is there any clipping? If not, how do you specify the trust region? (2) What about the normalization (e.g., tokenwise vs. sequence wise like in GSPO). (3) For the advantage, there is no division by std (is this coming from Dr. GRPO?) (4) For the expectation, it might be better to be more specific like: $\mathbb{E}_{x \sim \mathcal{D},\left\{a_i\right\}_{i=1}^K \sim \pi_{\theta_{\text {old }}}(\cdot \mid x, m)}$}
% \zixuan{will update to below, but perhaps these can go to appendix}

%%%%%%%%%%%%%%



% \begin{equation}
% \mathcal{L}_{\text{GRPO}}(\theta)
% =
% \mathbb{E}_{(x,y)}
% \left[
% \frac{1}{K}
% \sum_{i=1}^K
% \frac{\pi_\theta(a_i \mid x, m)}{\pi_{\theta_{\text{old}}}(a_i \mid x, m)}
% \cdot
% \bigl(R_i - \bar{R}\bigr)
% \right],
% \end{equation}

% \begin{equation}
% \mathcal{L}_{\text{GRPO}}(\theta)
% =
% \mathbb{E}_{(x,y)}
% \left[
% \frac{1}{K}
% \sum_{i=1}^K
% \left(
% \frac{\pi_\theta(a_i \mid x, m)}{\pi_{\theta_{\text{old}}}(a_i \mid x, m)}
% \cdot
% \bigl(R_i - \bar{R}\bigr)
% -
% \beta \,
% \mathrm{KL}\!\left(
% \pi_\theta(\cdot \mid x, m)
% \;\|\;
% \pi_{\theta_{\text{old}}}(\cdot \mid x, m)
% \right)
% \right)
% \right],
% \end{equation}

% \begin{equation}
% \label{eq:grpo}
% \begin{aligned}
% J_{\text{GRPO}}(\theta)
% =&\;
% \mathbb{E}_{x \sim \mathcal{D},\, \{a_i\}_{i=1}^{K} \sim \pi_{\theta_{\text{old}}}(\cdot \mid x,m)}
% \frac{1}{K}\sum_{i=1}^{K}\frac{1}{|a_i|}\sum_{t=1}^{|a_i|}
% \\
% &\qquad
% \Bigg\{
% \min\!\Bigg[
% r_{i,t}(\theta)\,
% \hat{A}_{i,t},\\
% &\;
% \mathrm{clip}\!\Bigg(
% r_{i,t}(\theta),\,
% 1-\epsilon,1+\epsilon
% \Bigg)\hat{A}_{i,t}
% \Bigg]
% \\
% &\qquad\qquad
% -\;\beta\, D_{\mathrm{KL}}\!\left(\pi_\theta \,\|\, \pi_{\mathrm{ref}}\right)
% \Bigg\}.
% \end{aligned}
% \end{equation}
%%%%%%%%%%%%%%

% This formulation treats agent orchestration as a contextual single-step reinforcement learning problem, where the policy is trained to propose effective multi-agent designs rather than individual low-level actions. \zixuan{TODO: briefly GRPO..}