%!TEX root = main.tex
\section{Conclusion}
\label{sec:conclusion}
In this work, we present \oursched, an efficient scheduler tailored for the uncertainty and hybridity characteristics of LLM workloads.
%for LLM inferences exhibiting uncertain and hybrid resource demands.
\oursched introduces a semantic-aware history-based predictor to predict output-length distribution in a light-weight and also accurate manner.
It also models the overall service cost of LLM inferences with both memory and compute bounds considered.
Besides, \oursched adopts an uncertainty-aware scheduling policy which leverages the Gittins index for the best queueing performance.
%-based scheduling policy to 
Testbed and simulation experiments confirm that \oursched can substantially improve the end-to-end efficiency performance, with an TTLT reduction of over 28.7\%.

% In this work, we presented \textsc{SageSched}, a Gittins-policy-based scheduling framework for LLM serving that tackles dynamicity and hybridity challenges end to end. \textsc{SageSched} infers per-request \emph{output-length distributions} in a training-free manner via semantic-aware retrieval over short-term serving history, models request cost with a unified formulation that captures KVCache-driven compute--memory coupling, and prioritizes requests using a Gittins-index policy with bucketed, runtime index refreshing and I/O-aware preemption to avoid excessive swapping. Implemented in a production-grade serving stack, \textsc{SageSched} improves end-to-end latency on diverse workloads. Experiments show that \textsc{SageSched} improves the average TTFT by approximately 28.7\% compared to the strongest baseline while remaining operationally lightweight.


